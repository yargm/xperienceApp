import 'package:flutter/material.dart';

class DestinoListScreen extends StatelessWidget {
  const DestinoListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.brown,
    );
  }
}
